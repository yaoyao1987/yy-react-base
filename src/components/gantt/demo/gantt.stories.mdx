import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks'
import GanttChart from '..'
import '../style'
import { data, cols, columns } from './demo'

<Meta title="业务组件/GanttChart " component={GanttChart} />

## 基本使用

<Preview>
  <Story name="甘特图">
    <div className="doc-wrapper" style={{ width: '100%', height: '60vh' }}>
      <GanttChart zoomLevel="W" data={data} cols={cols} columns={columns} />
    </div>
  </Story>
</Preview>

## API

通过设置 GanttChart 的 zoomLevel 来显示不同的头部

<Story name="API" />

<Props of={GanttChart} />
